services:
  localstack:
    container_name: aegismesh-localstack
    image: motoserver/moto:latest
    ports:
      - "127.0.0.1:4566:5000"
    environment:
      - AWS_DEFAULT_REGION=us-east-1
      - MOTO_PORT=5000
    restart: unless-stopped
